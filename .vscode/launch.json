{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Exécuter le script : dev",
            "request": "launch",
            "command": "npm run dev",
            "cwd": "${workspaceFolder}"
        }
    ]
}